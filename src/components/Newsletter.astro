<div>
  <section class={'section'}>
    <div>
      <header>
        <p class={'subtitle'}>Recibe gratis nuestra newsletter</p>
        <h2 class={'title'}>Recibe nuestra newsletter semanal</h2>
      </header>
      <div class={'description'}>
        <p>
          Recipe en tu correo, gratis, las mejores series y películas de forma
          semanal. Además te enviaremos los últimos artículos publicados en
          nuestra web.
        </p>
      </div>
      <iframe
        src='https://topdelmes.substack.com/embed'
        width='100%'
        height={150}
        frameborder='0'
        scrolling='no'></iframe>
    </div>
  </section>
</div>

<style>
  .section > div {
    max-width: 700px;
    margin: 0 auto;
    color: var(--gainsboro);
    font-size: var(--fs-7);
    padding: 32px;
    position: relative;
  }

  .section {
    text-align: center;
    margin: 0 auto;
    padding: 32px 0 0 0;
    border-top: 1px solid var(--gunmetal-2);
    background-color: #242c38;
  }

  .title {
    text-align: center;
    font-size: var(--fs-4);
    color: var(--white);
    line-height: 1.2;
  }

  .subtitle {
    color: var(--citrine);
    font-size: var(--fs-11);
    font-weight: var(--fw-500);
    text-transform: uppercase;
    letter-spacing: 1px;
    text-align: center;
  }
</style>
