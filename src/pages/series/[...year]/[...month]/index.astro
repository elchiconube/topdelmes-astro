---
import { validateYearAndMonth } from '../../../../utils';

const { year, month } = Astro.params;

if (!validateYearAndMonth(year, month, 'tv_series')) {
  return Astro.redirect('/404');
}
return Astro.redirect(`/mejores/series/${year}/${month}`);
---

<p>holi</p>
